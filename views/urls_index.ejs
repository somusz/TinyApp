<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <title>TinyApp</title>
</head>

<body>
  <% include partials/_header %>
  <main>
    <div>
      <ul>
        <% for (item in urls) { %>
          <li><%= item %> -> <a href='<%= urls[item] %>' target='blank'><%= urls[item] %></a>
          <form method='POST' action='/urls/<%= item %>/delete'>
            <a href='/urls/<%= item %>'>Edit</a>
            <button type="text">Delete</button>
          </form>
          </li>
        <% }; %>
      </ul>
      <a href='/urls/new'>Look, another short URL!</a>
    </div>

  </main>

</body>
</html>